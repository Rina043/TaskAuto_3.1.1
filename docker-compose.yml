version: '3.8'
services:
  mysql:
    image: postgres:13.1-alpine
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=postgresuser
      - POSTGRES_PASSWORD=pass123